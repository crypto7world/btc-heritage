# Login/Logout
heritage-cli [--datadir <datadir>] [--network <network>] service [--service-url <service_URL>] login
heritage-cli [--datadir <datadir>] [--network <network>] service [--service-url <service_URL>] logout

heritage-cli [--datadir <datadir>] [--network <network>] service [--service-url <service_URL>] list-wallets
heritage-cli [--datadir <datadir>] [--network <network>] service [--service-url <service_URL>] list-heirs
heritage-cli [--datadir <datadir>] [--network <network>] service [--service-url <service_URL>] list-heritages
heritage-cli [--datadir <datadir>] [--network <network>] service [--service-url <service_URL>] 
heritage-cli [--datadir <datadir>] [--network <network>] service [--service-url <service_URL>] 
heritage-cli [--datadir <datadir>] [--network <network>] service [--service-url <service_URL>] 
heritage-cli [--datadir <datadir>] [--network <network>] service [--service-url <service_URL>] 


heritage-cli [--datadir <datadir>] [--network <network>] list-wallets

heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> create \
    --online-wallet <none | service | electrum | bitcoin> [--url <service_url | electrum_url | btc_url>] \
    --offline-wallet <none | local | ledger> [--word-count <12|18|24>] [--with-password] \
    --no-account-xpubs-auto-feed
heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> restore \
    --online-wallet <none | service | electrum | bitcoin> [--url <service_url | electrum_url | btc_url>] [--existing-service-wallet <wallet_name>] \
    --offline-wallet <none | local | ledger> --seed <mnemonic> [--with-password] \
    --no-account-xpubs-auto-feed
heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> backup-online-wallet <backup-file-path>
heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> restore-online-wallet <backup-file-path>
heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> backup-offline-wallet <backup-file-path>

heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> balance
heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> sync









heritage-cli [--datadir <datadir>] local [--network <network>] wallet <wallet-name> restore <JSON-backup-file> [--seed <mnemonic> [--with-password] | --ledger]
heritage-cli [--datadir <datadir>] local [--network <network>] wallet

# Wallets sometime need to be restored, there is 2 fundamentaly different part to restore
# - the seed; for all things related to private keys
# - the descriptors; for all things related to TapRoot/Heritage configurations
# It will happen that only one of the two parts need restoration
# Furthermore, it's different for the service or for a local wallet

# The procedure will always to first ensure the "public" side is restored
heritage-cli service-wallet [--service-url <service_URL>] <wallet-name> restore <JSON-backup-file> [--seed <mnemonic> [--with-password] | --ledger]
heritage-cli local-wallet <wallet-name> restore <JSON-backup-file> [--seed <mnemonic> [--with-password] | --ledger]

# If the service-wallet already exist
heritage-cli service-wallet [--service-url <service_URL>] <wallet-name> rebind [--seed <mnemonic> [--with-password] | --ledger]


# Wallet setup
# Heritage configuration
heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> heritage-config set <...>
heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> heritage-config list
heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> heritage-config renew
heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> heritage-config ledger-register-current

# Account eXtended Public Keys
heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> account-xpubs feed
heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> account-xpubs list




# Wallet sync
heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> sync

# Wallet addresses
heritage-cli [--datadir <datadir>] [--network <network>] wallet <wallet-name> get-address

# Wallet PSBT/Sign/Broadcast
# Will generate the PSBT, present it for validation to the user, then sign and broadcast it

heritage-cli wallet [--service-url <service_URL>] <wallet-name> get-psbt --recipient <address>,<amount> [--sign] [--broadcast]
heritage-cli wallet [--service-url <service_URL>] <wallet-name> sign-psbt [--broadcast]
heritage-cli wallet [--service-url <service_URL>] <wallet-name> broadcast-psbt

heritage-cli local-wallet [--network <network>] [--bitcoin-url <btc_url> | --electrum-url <electrum_url> ] <wallet-name> broadcast-psbt
